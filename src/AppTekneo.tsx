import { RouterProvider } from "react-router";
import { routerTekneo } from "./app/routesTekneo";

function AppTekneo() {
  return <RouterProvider router={routerTekneo} />;
}

export default AppTekneo;
